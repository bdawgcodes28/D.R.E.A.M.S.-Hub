# RUNNING ON NODE CONTAINER RUNNING LINUX
FROM node:alpine

# CD INTO CONTAINER
WORKDIR /app

# COPY CONFIGURATION FILES TO CONTAINER /app DIRECTORY
COPY package*.json .

# COPY REST OF SOURCE FILES TO /app
COPY . .

# RUN INSTALL COMMANDS WHEN YOU PULL THE IMAGE
RUN npm install

# BUILD THE APPLICATION
RUN npm run build

# RUN COMMANDS TO START THE CONTAINER
EXPOSE 5173
CMD [ "npm", "run", "preview", "--", "--host", "0.0.0.0", "--port", "5173" ]
