


class ListNode:
    def __init__(self, val, next):
        self.val = val
        self.next = next


def reverse(head):
    prev = None
    curr = head
    while curr:
        temp = curr.next
        curr.next = prev
        prev = curr
        curr = temp
    while prev:
        print(prev.val)
        prev = prev.next
    
    

head = ListNode(1, ListNode(2, ListNode(3, ListNode(4, None))))
reverse(head)